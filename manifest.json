{
  "schemaVersion": 1,
  "id": "dielerorn.gamecenter",
  "name": "Game Center",
  "version": "0.1.0",
  "author": "Austin Larson",
  "license": "MIT",
  "repository": "https://github.com/Dielerorn/omarchy-game-center",
  "description": "The pre-game ritual in one panel — session toggles, controllers and instant replay, without six separate bar widgets",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Game Center",
    "description": "Session toggles, controllers and clip capture in one popout",
    "category": "Games",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "startTab": "session",
      "panelWidth": 380,
      "keepAwake": true,
      "silenceNotifications": true,
      "nightLightOff": true,
      "performanceProfile": true,
      "replaySeconds": 30,
      "replayStorage": "ram",
      "replayQuality": "balanced",
      "replayAudio": "desktop",
      "overlayCorner": "top-right",
      "overlayCpu": true,
      "overlayGpu": true,
      "overlayRam": true,
      "overlayVram": true,
      "overlayTemps": true,
      "overlayPower": false
    },
    "schema": [
      {
        "key": "startTab",
        "type": "enum",
        "label": "Opens showing",
        "options": [
          "session",
          "pads",
          "clips",
          "overlay"
        ],
        "defaultValue": "session",
        "description": "Which tab the panel is on when it opens. Also follows whichever tab you last left it on."
      },
      {
        "key": "panelWidth",
        "type": "integer",
        "label": "Panel width",
        "min": 300,
        "max": 640,
        "step": 10,
        "defaultValue": 380,
        "description": "In the shell's spacing units, so it follows your scale. Wider than the screen is trimmed to fit."
      },
      {
        "key": "keepAwake",
        "type": "boolean",
        "label": "Session keeps the screen awake",
        "defaultValue": true,
        "description": "Only ever claimed when stay-awake is off to begin with. If you turned it on yourself, or another plugin holds it, Game Center leaves it alone and says so in the panel."
      },
      {
        "key": "silenceNotifications",
        "type": "boolean",
        "label": "Session silences notifications",
        "defaultValue": true,
        "description": "Turns do-not-disturb on for the session and puts it back afterwards — unless you change it yourself mid-session, in which case your change wins."
      },
      {
        "key": "nightLightOff",
        "type": "boolean",
        "label": "Session turns off night light",
        "defaultValue": true,
        "description": "Restores the exact colour temperature you had, rather than whatever default hyprsunset comes back with."
      },
      {
        "key": "performanceProfile",
        "type": "boolean",
        "label": "Session sets the performance power profile",
        "defaultValue": true,
        "description": "Hidden on machines that have no performance profile, which is most desktops with a placeholder platform driver."
      },
      {
        "key": "replaySeconds",
        "type": "integer",
        "label": "Replay buffer length",
        "min": 5,
        "max": 300,
        "step": 5,
        "defaultValue": 30,
        "description": "Seconds kept in the buffer. Memory use scales with this and with your monitor: 30s of 1440p60 is roughly 300MB including the encoder's own working set."
      },
      {
        "key": "replayStorage",
        "type": "enum",
        "label": "Replay buffer held in",
        "options": [
          "ram",
          "disk"
        ],
        "defaultValue": "ram",
        "description": "RAM is instant and costs memory for the whole session. Disk trades that for writes to your drive."
      },
      {
        "key": "replayQuality",
        "type": "enum",
        "label": "Replay quality",
        "options": [
          "low",
          "balanced",
          "high",
          "ultra"
        ],
        "defaultValue": "balanced",
        "description": "Bitrate is scaled from this by your monitor's pixels and frame rate, so the same tier costs more on an ultrawide."
      },
      {
        "key": "replayAudio",
        "type": "enum",
        "label": "Replay audio",
        "options": [
          "desktop",
          "both",
          "none"
        ],
        "defaultValue": "desktop",
        "description": "'both' adds your microphone to the desktop audio."
      },
      {
        "key": "overlayCorner",
        "type": "enum",
        "label": "Overlay corner",
        "options": [
          "top-left",
          "top-right",
          "bottom-left",
          "bottom-right"
        ],
        "defaultValue": "top-right",
        "description": "Which corner the in-game stats sit in, on whichever screen the game is on."
      },
      {
        "key": "overlayCpu",
        "type": "boolean",
        "label": "Overlay shows CPU load",
        "defaultValue": true
      },
      {
        "key": "overlayGpu",
        "type": "boolean",
        "label": "Overlay shows GPU load",
        "defaultValue": true
      },
      {
        "key": "overlayRam",
        "type": "boolean",
        "label": "Overlay shows system memory",
        "defaultValue": true
      },
      {
        "key": "overlayVram",
        "type": "boolean",
        "label": "Overlay shows VRAM",
        "defaultValue": true
      },
      {
        "key": "overlayTemps",
        "type": "boolean",
        "label": "Overlay shows temperatures",
        "defaultValue": true,
        "description": "Amber past 80°C, red past 90°C."
      },
      {
        "key": "overlayPower",
        "type": "boolean",
        "label": "Overlay shows GPU power draw",
        "defaultValue": false,
        "description": "NVIDIA only; the AMD sysfs path doesn't report it."
      }
    ]
  },
  "keepLoaded": true
}
